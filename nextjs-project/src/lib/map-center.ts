/**
 * Координаты шоурума [широта, долгота] — вынесены из yandex-map.tsx (там 'use client'),
 * чтобы серверные компоненты (например, страница контактов) могли использовать
 * реальное значение, а не клиентскую ссылку-заглушку.
 */
export const MAP_CENTER: [number, number] = [55.78284, 37.45149]
