# Upstream — Next.js app in Docker
upstream app {
  server app:3000;
  keepalive 32;
}

# HTTP server (redirect to HTTPS when SSL is enabled)
server {
  listen 80;
  server_name _;

  # For Let's Encrypt / certbot
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
    allow all;
  }

  # Uncomment after SSL is set up to redirect HTTP -> HTTPS:
  # return 301 https://$host$request_uri;

  location / {
    proxy_pass http://app;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;
  }
}

# HTTPS server — uncomment and set paths to certs when ready
# server {
#   listen 443 ssl http2;
#   server_name innerhealth.ru www.innerhealth.ru;
#
#   ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#   ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#   ssl_session_timeout 1d;
#   ssl_session_cache shared:SSL:50m;
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#
#   location / {
#     proxy_pass http://app;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Connection "";
#     proxy_buffering off;
#     proxy_read_timeout 60s;
#     proxy_send_timeout 60s;
#   }
# }
