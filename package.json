{
  "name": "innerhealth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix nextjs-project run dev",
    "build": "npm --prefix nextjs-project run build",
    "start": "npm --prefix nextjs-project run start",
    "lint": "npm --prefix nextjs-project run lint"
  },
  "dependencies": {
    "@prisma/client": "^7.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.4.19",
    "postcss": "^8",
    "prisma": "^7.4.0",
    "typescript": "^5"
  }
}
