# Task: Route Implementation

## Paths:
- Static: `/o-nas`, `/sertifikaty-sootvetstviya`, `/contacts`, `/privacy`, `/oferta`, `/sotrudnichestvo`, `/otzyvy`, `/informaciya`.
- Collections: `/collagen`, `/gribnaya-kollektsiya`, `/peptidy-kostnogo-buliona`, `/nutrienty`, `/akcii`, `/podarochnye-nabory`.
- **Sitemap:** Авто-генерация `sitemap.ts` на основе данных из Prisma.